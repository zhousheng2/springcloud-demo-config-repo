feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000
        loggerLevel: basic 
# eureka:
#   client:
#     service-url:
#       defaultZone: http://localhost:8762/eureka/
env:
  test4
girl:
  name: mo
  age: 18
